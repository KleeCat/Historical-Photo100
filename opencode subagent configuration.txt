# OpenCode Subagent Configuration (Sanitized)

This file is a redacted version of a troubleshooting transcript.
All secrets, personal paths, host details, and runtime identifiers were removed.

## Scope

- Goal: make OpenCode CLI subagents usable through a local proxy.
- Result: subagents worked after local proxy routing and model-name rewrite.
- Security: all plaintext credentials removed and replaced with placeholders.

## Redaction Policy Applied

- API keys/tokens -> `<REDACTED_API_KEY>` / `<REDACTED_TOKEN>`
- Authorization headers -> `<REDACTED_AUTH_HEADER>`
- Local absolute paths -> `<REDACTED_LOCAL_PATH>`
- Username/host fingerprints -> `<REDACTED_USER_HOST>`
- Internal endpoints/IP:port -> `<REDACTED_INTERNAL_ENDPOINT>`
- Process IDs and transient runtime IDs -> `<REDACTED_RUNTIME_ID>`

## Sanitized Technical Notes

1. A local reverse proxy was used to forward model API calls.
2. Short model names were mapped to full provider model IDs.
3. Base URL in local client config was updated to local proxy endpoint.
4. Startup automation was tested using local startup scripts.
5. Connectivity was validated with HTTP status checks and test completions.

## Sanitized Configuration Template

Use this template pattern only (no plaintext secrets in repo files):

```json
{
  "provider": {
    "npm": "@ai-sdk/openai-compatible",
    "models": {
      "model-a": {},
      "model-b": {}
    },
    "options": {
      "apiKey": "{env:PROVIDER_API_KEY}",
      "baseURL": "<REDACTED_INTERNAL_ENDPOINT>/v1"
    }
  }
}
```

## Operational Safety Checklist

- Never store plaintext keys in chat logs, markdown, or txt artifacts.
- Keep credentials in environment variables only.
- Avoid printing secret-bearing commands in terminal transcripts.
- If a key is exposed, rotate it immediately.
- Before commit/push, run secret scan on working tree and git history.

## Cleanup Record

- Plaintext key artifacts removed from this file.
- Sensitive runtime traces replaced with placeholders.
- File retained as sanitized documentation only.
